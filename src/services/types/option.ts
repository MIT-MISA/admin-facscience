export type options = {
    postAffectations: string[]
    grades: string[]
    fonctions: string[]
    titres: string[]
    appartenances: string[]
    responsabilites: string[]  
}

export type option = {
    nom : string
}